# Quick check

For now this is plain markdown.

If you want quizzes to be editable later by non-technical users, the best approach is to store quiz questions in JSON (rather than embedding them in markdown) and have the app render them consistently.
